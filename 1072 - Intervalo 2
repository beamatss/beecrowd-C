#include <stdio.h>

int main()
{
    int N;
    scanf("%d", &N);
    int num[N];
    int contador_in = 0;
    int contador_out = 0;
    for (int i = 0; i < N; i++){
        scanf("%d", &num[i]);
        if (num[i] >=10 && num[i] <= 20){
            contador_in += 1;
        }else{
            contador_out+= 1;
        }
    }
    printf("%d in\n", contador_in);
    printf("%d out\n", contador_out);

    return 0;
}
